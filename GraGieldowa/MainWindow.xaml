<Window
    x:Class="GraGieldowa.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GraGieldowa"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top">
            <!--<Button x:Name="myButton" Click="myButton_Click">Click Me</Button>-->
            <Button Background="White" x:Name="AddUserButton" Content="Dodaj nowego użytkownika" Click="AddUser_Click" />

            <ComboBox Background="White" x:Name="UsersComboBox" SelectionChanged="UserComboBox_SelectionChanged" Header="Użytkownicy" PlaceholderText="Wybierz użytkownika" Width="200">
                <ListView ItemsSource="{x:Bind ViewModel, Mode=OneWay}"
                          DisplayMemberPath="UserName"
                          />
            </ComboBox>

        </StackPanel>
        <Frame  Background="Yellow"
                Grid.Row="1"
                x:Name="frame"
                VerticalAlignment="Center"
               />
    </Grid>
</Window>
